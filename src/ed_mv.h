#ifndef _ED_MV_H
#define _ED_MV_H

/* Moves editor to the beginning of the file. */
void ed_mv_to_begin_of_file(Ed *);

/* Moves editor to the beginning of the row. */
void ed_mv_to_begin_of_row(Ed *);

/* Moves editor to the end of the file. */
void ed_mv_to_end_of_file(Ed *);

#endif /* _ED_MV_H */
