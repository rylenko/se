#ifndef _MATH_H
#define _MATH_H

#define MAX(x, y) ((x) > (y) ? (x) : (y))
#define MIN(x, y) ((x) < (y) ? (x) : (y))

#ifndef SIZE_MAX
#define SIZE_MAX ((size_t)(-1))
#endif /* SIZE_MAX */

#endif /* _MATH_H */
